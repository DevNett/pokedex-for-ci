name: Say hello

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Echo the message
        if: ${{ github.event_name == 'push' }} 
        run: |
          echo "Hola midudevs! ${{secrets.HEROKU_API_KEY}}"
      - name: Dime la fecha
        run: date
      - run: ls -l